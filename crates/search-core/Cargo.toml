[package]
name = "allerx-search-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "AllerX business logic — ranking, matching, repository contract. No sqlx, testable with a mock repository"

[dependencies]
allerx-models = { path = "../models" }
async-trait.workspace = true
chrono.workspace = true   # NaiveDate in the mock's concurrent-medications dedupe
thiserror.workspace = true

[dev-dependencies]
tokio.workspace = true
